import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    borderWidth: 1,
    borderColor: '#969696',
    width: '100%',
    paddingLeft: 15,
    borderRadius: 6
  },
  picker: {
    flex: 1,
    fontSize: 14
  }
});
