import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: 110,
    alignItems: 'center',
    gap: 24
  },
  descriptionText: {
    maxWidth: 260,
    fontSize: 14,
    textAlign: 'center',
    fontFamily: 'Inter_400Regular'
  }
});
